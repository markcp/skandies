<header>
  <%= link_to "Home", root_path, id: "home-link" %>

  <h1>Skandies <%= @year.name %></h1>

  <ul id="results-menu">
    <li><%= link_to "Results", results_category_path(Category.best_picture, year: @year.name) %></li>
    <li><%= link_to "Ratings", results_movies_path(year: @year.name) %></li>
    <li><%= link_to "Participants", results_ballots_path(year: @year.name) %></li>
    <li class="last"><%= link_to "Top Tens", results_top_ten_entries_path(year: @year.name) %></li>
  </ul>
</header>
