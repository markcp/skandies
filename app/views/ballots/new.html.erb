<%= simple_form_for(@ballot) do |ballot_form| %>
  <%= ballot_form.input :user_id, as: :hidden %>
  <%= ballot_form.input :year_id, as: :hidden %>
  <%= ballot_form.submit "Create new " + @voting_year.name + " ballot", class: "button" %>
<% end %>
